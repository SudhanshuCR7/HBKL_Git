Igniting our application 

Command -  npm install -D parcel

Difference between package.json and package-lock.json
Package-lock.json - 
locks the version of packages
Never to put in git ignore
 

Tilde and caret in the package version.

CDN links to get react is not a good method because in that url for ex - react18 is mentioned and what if react moves to 19, also in that way react and reactDOM will not be the part of node_modules.